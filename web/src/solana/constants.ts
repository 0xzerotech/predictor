export const PROGRAM_ID = null;
export const GLOBAL_SEED = new Uint8Array();
export const MARKET_SEED = new Uint8Array();
export const CURVE_SEED = new Uint8Array();
export const QUOTE_VAULT_SEED = new Uint8Array();
export const ATTENTION_VAULT_SEED = new Uint8Array();
export const RESOLUTION_SEED = new Uint8Array();
export const DEFAULT_ENDPOINT = "";
export const DEFAULT_CLUSTER = "";

